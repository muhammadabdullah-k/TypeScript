"use strict";
//QUESTION-12
//Greetings: Start with the array you used in Exercise 11, but instead of just printing each person’s name, print a message to them. The text of each message should be the same, but each message should be personalized with the person’s name.
let _names = ["jack", "barney", "tom", "alex", "homer"];
console.log(`${_names[0]} lives near our School`);
console.log(`${_names[1]} is a dinosaur`);
console.log(`${_names[2]} likes to drive cars`);
console.log(`${_names[3]} is visiting Canada`);
console.log(`${_names[4]} likes donuts`);
console.log('\n');
